## 问题分析

`.child-row` 类的样式可能被 Ant Design Vue 表格的默认样式覆盖，导致空白过大。

## 解决方案

1. **使用更高优先级的选择器**：`.ant-table-tbody tr.child-row td`
2. **减小 padding 并设置 line-height**：使内容更紧凑
3. **使用 `!important` 确保样式生效**

修改 devices.vue 的样式部分。