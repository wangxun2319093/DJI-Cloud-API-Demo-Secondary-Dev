## 分析结论

备份项目中 `height: 0` 是正确的设置，因为：
- 多边形是半透明的（alpha: 0.3）
- 扫描路径在 50 米高度
- 从上方俯视时，可以同时看到地面上的多边形和上方的扫描路径

我之前的修改方向是错误的。

## 解决方案

回退 CesiumMap.vue 到备份项目的状态：

1. **恢复多边形高度**：`height: 0`

2. **移除额外的 watch 监听**：
   - 移除 waypoints 变化时的 `renderScanPath()`
   - 移除 routeType 变化监听

这样可以让代码恢复到备份项目的正确状态。