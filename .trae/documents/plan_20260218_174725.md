## DSM 功能添加详细修改计划

### 一、MySQL 数据库

#### 1. 创建 manage_map 表
```sql
CREATE TABLE `manage_map` (
  `id` int(10) unsigned NOT NULL AUTO_INCREMENT,
  `map_id` varchar(64) NOT NULL COMMENT '地图唯一标识',
  `map_name` varchar(128) NOT NULL COMMENT '地图名称',
  `workspace_id` varchar(64) NOT NULL COMMENT '工作空间ID',
  `user_id` varchar(64) NOT NULL COMMENT '用户ID',
  `user_name` varchar(64) NOT NULL COMMENT '用户名',
  `object_key` varchar(500) NOT NULL COMMENT 'MinIO存储路径',
  `file_size` bigint(20) DEFAULT 0 COMMENT '文件大小',
  `map_format` varchar(32) NOT NULL COMMENT '地图格式(terrain/3dtiles)',
  `status` tinyint(1) NOT NULL DEFAULT 0 COMMENT '状态: 0-处理中,1-可用,2-失败',
  `min_lng` decimal(10,6) DEFAULT NULL COMMENT '最小经度',
  `max_lng` decimal(10,6) DEFAULT NULL COMMENT '最大经度',
  `min_lat` decimal(10,6) DEFAULT NULL COMMENT '最小纬度',
  `max_lat` decimal(10,6) DEFAULT NULL COMMENT '最大纬度',
  `min_elevation` decimal(10,2) DEFAULT NULL COMMENT '最小高程',
  `max_elevation` decimal(10,2) DEFAULT NULL COMMENT '最大高程',
  `description` varchar(500) DEFAULT NULL COMMENT '描述',
  `create_time` bigint(