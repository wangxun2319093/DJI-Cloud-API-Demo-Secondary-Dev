## 问题分析

`.child-row` 设置了固定高度 `40px`，但表格单元格的 padding 是 `8px`，导致：
- 内容区域被 padding 压缩
- 固定高度造成额外空白

## 解决方案

**移除固定高度，改用 padding 控制行高**

1. 删除 `.child-row` 的固定高度
2. 调整表格单元格 padding 为 `12px 16px`（上下 12px，左右 16px）
3. 保持字体大小 14px

这样行高会自动适应内容，不会有多余空白。