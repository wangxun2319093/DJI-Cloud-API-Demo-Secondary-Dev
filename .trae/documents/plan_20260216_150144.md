## 增强 Online Devices (Gateway) 状态显示

### 1. 更新前端类型定义 (`device.ts`)

扩展 `GatewayOsd` 接口，添加完整的 `wireless_link` 字段：
- `sdr_freq_band` - SDR 频段
- `sdr_link_state` - SDR 链路状态
- `sdr_quality` - SDR 信号质量

### 2. 增强 Online Devices 面板 UI (`tsa.vue`)

仿照 Dock 面板的样式，增加以下状态信息显示：

```
┌─────────────────────────────────────┐
│ DJI RC Plus - DJI RC Plus           │  ← 设备名称
├─────────────────────────────────────┤
│ 🚀 Online                           │  ← 在线状态
│ 📡 4G: Q4  📶 SDR: Q5              │  ← 4G/SDR 信号质量
│ 🔋 95%  📍 30.5728, 114.2793       │  ← 电量/位置
│ 📶 Link: 4G+SDR  ✅ 连接正常        │  ← 链路模式/状态
└─────────────────────────────────────┘
```

### 3. 添加辅助函数

- `get4GQualityColor()` - 4G 信号质量颜色
- `getSDRQualityColor()` - SDR 信号质量颜色
- `getLinkModeText()` - 链路模式文本

### 4. 更新模拟器

确保模拟器发送完整的 `wireless_link` 数据（包括 SDR 字段）